% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-snfi-volume.R
\name{silv_predict_snfi_volume}
\alias{silv_predict_snfi_volume}
\title{Predict tree volume using Spanish National Forest Inventory equations}
\usage{
silv_predict_snfi_volume(
  province,
  species,
  dbh = NULL,
  h = NULL,
  dnm = NULL,
  quality = "default",
  quiet = FALSE
)
}
\arguments{
\item{province}{A character or numeric vector. Province name (e.g.
\code{"Cantabria"}) or province code (e.g. \code{39}). See \code{\link[=silv_snfi_provinces]{silv_snfi_provinces()}}.}

\item{species}{A character or numeric vector. Species scientific name (e.g.
\code{"Pinus radiata"}) or SNFI species code (e.g. \code{28}). See
\code{\link[=silv_snfi_species]{silv_snfi_species()}}.}

\item{dbh}{A numeric vector of diameter at breast height in \strong{cm}.}

\item{h}{A numeric vector of tree heights in \strong{m}. Optional for some
models (e.g. SNFI3 model 14), but required by most VCC equations.}

\item{dnm}{A numeric vector of mean plot diameter in \strong{cm}. Required only
when the IAVC equation uses model 13.}

\item{quality}{A character or numeric vector. Quality/shape class (\code{1}-\code{6}).
Use \code{"default"} (the default) to select the first available class.}

\item{quiet}{A logical value. If \code{TRUE}, suppresses citation messages.
Defaults to \code{FALSE}.}
}
\value{
A \code{data.frame} with one row per input tree and five columns:
\describe{
\item{\code{vcc}}{Merchantable volume \strong{with} bark (dm^3).}
\item{\code{vsc}}{Merchantable volume \strong{without} bark (dm^3).}
\item{\code{iavc}}{Annual volume increment with bark (dm^3).}
\item{\code{vle}}{Coarse firewood volume (dm^3).}
\item{\code{snfi_version}}{Dataset used: \code{"SNFI4"} or \code{"SNFI3"}.}
}
Any component for which parameters are missing or a required input is
\code{NA} returns \code{NA} for that column, without stopping execution.
}
\description{
Computes four volume metrics (in dm^3) for one or more trees using the
allometric equations from the 3rd and 4th Spanish National Forest Inventory
(SNFI3/SNFI4), as documented by MITECO (Anexo 19).
}
\details{
\subsection{Model selection}{

For each province, SNFI4 data are preferred. When a province is not covered
by SNFI4 (11 provinces), the function falls back to SNFI3 automatically.
SNFI3 also provides a supplementary file for species not present in the
standard provincial tables.
}

\subsection{Equation models}{\tabular{ll}{
   Component \tab Models used \cr
   VCC \tab 1, 11 \cr
   VSC \tab 7 \cr
   IAVC \tab 8, 13, 14, 16, 17, 18, 19, 20, 21, 25 \cr
   VLE \tab 10, 12 \cr
}


Full model equations are available in Anexo 19 of the MITECO SIG
documentation (see References).
}

\subsection{Units}{

Inputs: \code{dbh} and \code{dnm} in \strong{cm} (converted to mm internally),
\code{h} in \strong{m}. All outputs in \strong{dm^3}.
}
}
\examples{
# Single tree: Pinus radiata in Alava (province 1), code 28
silv_predict_snfi_volume(
  province = 1,
  species  = 28,
  dbh      = 20,
  h        = 15,
  dnm      = 23
)

# Mixed inputs: province and species by name
silv_predict_snfi_volume(
  province = c(1, 39),
  species  = c("Pinus radiata", "Pinus radiata"),
  dbh      = c(20, 25),
  h        = c(15, 18),
  dnm      = c(23, 26)
)
}
\references{
MITECO. 4th Spanish National Forest Inventory - SIG database codes.
\url{https://www.miteco.gob.es/content/dam/miteco/es/biodiversidad/temas/inventarios-nacionales/documentador_sig_tcm30-536622.pdf}

MITECO. 3rd Spanish National Forest Inventory - SIG database codes.
\url{https://www.miteco.gob.es/content/dam/miteco/es/biodiversidad/servicios/banco-datos-naturaleza/documentador_bdcampo_ifn3_tcm30-282240.pdf}
}
\seealso{
\code{\link[=silv_snfi_provinces]{silv_snfi_provinces()}}, \code{\link[=silv_snfi_species]{silv_snfi_species()}}, \code{\link[=silv_tree_volume]{silv_tree_volume()}}
}
